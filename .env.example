# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role must NEVER be shipped to client bundles
SUPABASE_SERVICE_ROLE_KEY=

# Base path for GitHub Pages (empty locally, '/<repo>' on Pages)
NEXT_PUBLIC_BASE_PATH=
